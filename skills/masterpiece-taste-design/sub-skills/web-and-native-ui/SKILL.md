---
name: web-and-native-ui
description: Поднавык фронтенд-инженерии и нативного интерфейса. Стабильность viewport, CSS Grid, доступность клавиатуры, reduced motion и эргономика мобильных устройств.
---

# Sub-Skill: Инженерия веб-интерфейсов и нативных приложений

Технические стандарты верстки интерфейсов с акцентом на надежность, доступность и адаптивность.

---

## 1. Базовые правила веб-инженерии

### 1.1 Стабильность высоты экрана
- Подход content-first: высота экрана определяется естественным потоком контента; не требовать принудительную полноэкранную фиксацию для всех экранов подряд.
- При обоснованной полноэкранной верстке: единица `dvh` динамически изменяется при скрытии/появлении панелей браузера (что может вызывать сдвиг контента при скролле), тогда как `svh` рассчитывается по наименьшему размеру и обеспечивает стабильность первого экрана без скачков. Выбирать единицу под конкретный UX-сценарий.

### 1.2 Сетка и адаптивность
- Для сложных структур использовать CSS Grid (`grid grid-cols-1 md:grid-cols-3 gap-6`), обеспечивающий предсказуемое выравнивание колонок.
- Флекс-контейнеры (`flex`) применять для линейных цепочек элементов (панели действий, списки тегов, шапки).

### 1.3 Доступность с клавиатуры (Keyboard Accessibility)
Все интерактивные элементы обязаны иметь четкое состояние `:focus-visible`:
```css
:focus-visible {
  outline: 2px solid var(--border-interactive, #2563eb);
  outline-offset: 2px;
}
```

### 1.4 Учет prefers-reduced-motion
Для пользователей с чувствительностью к анимации интерфейс обязан отключать или минимизировать движение:
```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
}
```

### 1.5 Стандарт доступных модальных окон (WAI-ARIA Focus Trap)
Любое модальное окно обязано реализовывать:
1. Захват фокуса (`ref.current?.focus()`) и циклическую навигацию по `Tab` / `Shift+Tab`.
2. Закрытие по нажатию клавиши `Escape`.
3. Блокировку скролла страницы (`document.body.style.overflow = 'hidden'`).
4. Обязательный возврат фокуса на элемент, открывший модальное окно, при его закрытии.
5. Семантические атрибуты: `role="dialog"`, `aria-modal="true"`, `aria-label={title}`.
(Готовый React-компонент: см. [../../references/archive_1_masterpiece_exemplar.md](../../references/archive_1_masterpiece_exemplar.md)).

---

## 2. Мобильная эргономика (Touch Ergonomics)

1. **Размер кликабельной зоны:** 44x44px — проектный ориентир для отдельных интерактивных контролов (кнопок, иконок действий), но не безусловная норма WCAG для inline-ссылок в связном тексте.
2. **Псевдоэлемент расширения кликабельной зоны:** для визуально компактных кнопок (например, 25x25px) использовать `::before` с размером 44x44px.
3. **Безопасные зоны экрана:** учет отступов `env(safe-area-inset-bottom)` и `env(safe-area-inset-top)` для фиксированных и плавающих панелей.
4. **Строка CTA в мобильном интерфейсе:** кнопка целевого действия должна быть комфортно доступна в зоне естественного охвата экрана.

---

## 3. Честный статус вместо декоративных точек

- Индикаторы состояния (`[OK]`, `[В работе]`, `[Завершено]`) должны отражать реальное состояние данных.
- Запрещено добавлять декоративные пульсирующие зеленые круги с надписью «All systems operational» на сайтах, не имеющих отношения к системному мониторингу.

---

## 4. Кинетическая инженерия и оптика поверхностей

### 4.1 Стандарт кривых ускорения и таймингов
- **Мгновенный отклик кнопок (150–200ms):** `transition-all duration-200 ease-out` либо `active:scale-[0.98]`.
- **Вход модальных окон, дропдаунов и карточек (300–350ms):** `cubic-bezier(0.16, 1, 0.3, 1)` (плавный быстрый разгон и бесшовное торможение без перелета).
- **Полноэкранные переходы и шторки (500–600ms):** `cubic-bezier(0.22, 1, 0.36, 1)`.

### 4.2 Оптика матового стекла (Liquid Glass)
Полупрозрачные поверхности строятся с компенсацией сатурации, чтобы избежать размытия цвета в блеклую серую муть:
```css
.liquid-glass {
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid rgba(255, 255, 255, 0.10);
  backdrop-filter: blur(20px) saturate(140%);
  -webkit-backdrop-filter: blur(20px) saturate(140%);
  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.08), 0 10px 30px rgba(0, 0, 0, 0.25);
}
```

---

## 5. Запрет карго-культа и эталонный образец

- **Запрет мета-комментирования:** Не добавлять в код комментарии с терминами скилла (`/* Anti-Slop Grid */`, `/* Kinetic discipline */`).
- **Эталонный проект Архива 1 («Ветер»):** полный разбор кода и архитектурных стандартов см. в [../../references/archive_1_masterpiece_exemplar.md](../../references/archive_1_masterpiece_exemplar.md).
- Руководство по физике движения и пружинам: [../../references/motion_design_and_interactions.md](../../references/motion_design_and_interactions.md).


